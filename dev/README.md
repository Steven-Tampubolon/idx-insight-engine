# dev/

Folder utility scripts untuk development dan debugging.
File-file ini tidak diperlukan untuk menjalankan aplikasi utama.

- `test_logic.py` — test `get_company_details()` secara langsung
- `check_groq.py` — cek model yang tersedia di Groq API key